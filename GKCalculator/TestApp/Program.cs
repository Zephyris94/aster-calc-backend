using System;
using System.Collections.Generic;
using System.Linq;
using Core;
using Model;

namespace TestApp
{
    class Program
    {
        static void Main(string[] args)
        {
            //Console.WriteLine("Hello World!");

            //var parsedData = ExcelParser.ParseExcel();

            //GraphConverter.InitTpTable(parsedData);
            //var graph = GraphConverter.GetGraph();

            //Console.WriteLine("Input point A");

            //var a = Console.ReadLine();

            //Console.WriteLine("Input point B");

            //var b = Console.ReadLine();

            //var dijkstra = new Dijkstra(graph);
            //var path = dijkstra.FindShortestPath(a, b);

            //var nodes = path.Split(";");

            //var results = new List<PathModel>();
            //for (int i = 0; i < nodes.Length - 1; i++)
            //{
            //    var edge = parsedData.FirstOrDefault(x => x.Source == nodes[i] && x.Destination == nodes[i + 1]);
            //    results.Add(edge);
            //}

            //foreach (var el in results)
            //{
            //    //Console.WriteLine($"{el.Source} -> {el.Destination} for {el.Price}$. " + (el.IsWyvern ? "Use wyvern" : "Use GK"));
            //}

            //Console.WriteLine("Total Price: " + results.Sum(x=>x.Price));
        }
    }
}
